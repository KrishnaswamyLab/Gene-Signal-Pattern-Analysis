To get predictions of localization from embedding, as well as compare Spearman correlation between spread and distance to uniform signal embedding, run:

```
./comparison.sh
```

Then `1.0_performance_comparisons.ipynb` to generate figures.
